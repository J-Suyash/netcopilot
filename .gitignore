# secrets — never commit
.env

# python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# runtime artifacts
*.jsonl
audit/

# editors / tools  (slash-less too: these are files on some clones, C19)
.idea/
.idea
.vscode/
.vscode
.mcp.json
.ripgreprc

# stray shell dotfiles that ended up in the repo root on the dev clone (N25)
.bashrc
.zshrc
.gitconfig
.bash_profile
.profile
.zprofile
.claude/
